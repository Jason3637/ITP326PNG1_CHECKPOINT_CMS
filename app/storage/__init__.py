"""Supabase Storage helpers.

Populated in Phase B4. Will expose a small wrapper around the official
``supabase`` Python client for uploading/downloading member documents
(IDs, loan agreements, receipts) to the bucket named by
``SUPABASE_STORAGE_BUCKET``.
"""
